[user]
	name = Ras96
	email = asymptote.k.k@gmail.com
[push]
	default = current
[core]
	editor = vim
	pager = delta --light
[fetch]
	prune = true

[alias]
	bprune = !git branch --merged | egrep -v '\\*|develop|main|master' | xargs git branch -d
	co = commit
	br = branch -a
	e = !gitmoji -c
	ae = !git add . && gitmoji -c
	lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n'' %C(black)%s%C(reset) %C(dim white)- %an%C(reset)' --all
	lo = log --oneline

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true  # use n and N to move between diff sections
	side-by-side = true
	line-numbers = true

[merge]
	conflictstyle = diff3

[diff]
	colorMoved = default

